# ---------------------------------------------------------------------------
# H2 in-memory profile. Activate with:  --spring.profiles.active=h2
# Data is wiped on every restart. Useful for quick testing without MySQL.
# Browse it at http://localhost:7478/h2-console (JDBC URL below, user "sa").
# ---------------------------------------------------------------------------
spring.datasource.url=jdbc:h2:mem:springbootproject;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
